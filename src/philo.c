#include "philo.h"

int main(int argc, char **argv)
{
    // t_list philo;

    // philo.count_ph = argv[1];
    // philo.time_to_die = argv[2];
    // philo.time_to_eat = argv[3];
    // philo.time_to_sleep = argv[4];
    // philo.nb_each_ph_eat = argv[5];

    printf("%d %s", argc, argv[0]);
    return (0);
}